import type { AiSpecEntry } from '~ims-app-base/logic/types/AiSpec';

export const localeBlockAiSpec: AiSpecEntry = {
  name: 'locale',
  icon: 'table-2',
  aiSpec: {
    brief:
      'Translation management for the entire asset. System-internal block, auto-created when localization is enabled.',
  },
};
